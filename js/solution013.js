// Volume of a Cuboid

class Kata {
  static getVolumeOfCuboid(length, width, height) {
    // P: three integers
    // R: integer
    // E: getVolumeOfCuboid(2, 2, 2) returns 8
    // P:
    // returns l * w * h

    return length * width * height;
  }
}
